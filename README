drupal 7 distribution

To install this profile using drush make, run the following commands:

cat > /tmp/drupaldist.make << EOF
core = 7.x
api = 2

projects[drupal] = 7.7

projects[drupaldist][type] = "profile"
projects[drupaldist][download][type] = "git"
projects[drupaldist][download][url] = "git@github.com:acrollet/drupal_dist.git"
EOF
drush make /tmp/drupaldist.make make
